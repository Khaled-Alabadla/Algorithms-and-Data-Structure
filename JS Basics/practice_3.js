// Write a function called welcome that prints "Welcome to coding!" when called.
function sayWelcome() {
  console.log("Welcome to coding!");
}

// Write a function called square that takes a number as input and prints its square.
function calcSquare(number) {
  console.log(number * number);
}

// Write a function called convertToCelsius that takes a temperature in Fahrenheit and returns the temperature in Celsius.
function convertToCelsius(f) {
  return ((f - 32) * 5) / 9;
}

// Write a function called isEven that checks if a number is even or odd and returns true or false.
function isEven(number) {
  return number % 2 == 0;
}
